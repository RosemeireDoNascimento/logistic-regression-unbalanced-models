---
title: "Sim_funcoes_logistica_cutoff_creditcard"
author: "Rosemeire do Nascimento"
date: "22/02/2026"
output:
  word_document: default
  pdf_document: default
---


```{r}
# 1. CARREGAMENTO DE PACOTES
library(tidyverse)
library(ggplot2)
library(caret)    
library(maxLik)   
library(powdist)  
library(pROC)
library(PRROC)
library(future)
library(future.apply)

```







```{r}
# 1. Configurações e variáveis globais ---------------------------------------
set.seed(1234)

# Epsilon para estabilidade numérica
EPS_PROB <- 1e-12

# Configurações de memória e paralellismo
plan(multicore, workers = parallel::detectCores() - 1)

options(future.globals.maxSize = 8 * 1024^3) # 8GB para lidar com o dataset

cat("Configuração paralela: multicore (Linux) com", parallel::detectCores() - 1, "workers.\n")

```







```{r}
# 2. Carregar base real -------------------------------------------------------

dados <- read.csv("creditcard.csv")
dim(dados)
str(dados)
summary(dados)

# Padronizar variáveis e tratar Class
dados$Class <- factor(dados$Class, levels = c(0, 1))
dados$Amount <- as.numeric(scale(dados$Amount))
dados$Time   <- as.numeric(scale(dados$Time))
dados$logAmount <- log1p(dados$Amount) # Mantendo sua transformação

#Análise da Variável Resposta (Desbalanceamento)
table(dados$Class)
prop.table(table(dados$Class))


#Distribuição do Valor das Transações
summary(dados$Amount)
hist(dados$Amount, breaks = 100)
boxplot(dados$Amount ~ dados$Class)

dados$logAmount <- log1p(dados$Amount)

#Correlação entre Variáveis
cor_mat <- cor(dados[,1:28])

#Comparação de Médias entre Classes
aggregate(. ~ Class, data = dados[, c(2:30, 31)], mean)
#Teste individual:
t.test(V1 ~ Class, data = dados)

#Métricas de Separação Inicial
roc_obj <- roc(dados$Class, dados$V1)
auc(roc_obj)

N_DATA <-nrow(dados)

# Padronizar variáveis que não são PCA
dados$Amount <- as.numeric(scale(dados$Amount))
dados$Time   <- as.numeric(scale(dados$Time))


#Objetos derivados da base ----------------------------------------------------

predictors <- setdiff(names(dados), "Class")

P_FEATURES <- length(predictors)

form_glm <- as.formula(
  paste("Class ~", paste(predictors, collapse = "+"))
)

n_total   <- nrow(dados)
prop_real <- mean(as.numeric(as.character(dados$Class)) == 1)

cat("--------------------------------------------------\n")
cat("Resumo da Preparação:\n")
cat("Número total de observações:", n_total, "\n")
cat("Número de preditores:", P_FEATURES, "\n")
cat("Proporção real de fraudes:", round(prop_real * 100, 4), "%\n")
cat("--------------------------------------------------\n")

```







```{r}
# 3. Funções utilitárias -----------------------------------------------------
# Extrai métricas de confusionMatrix (com proteção quando cm é NULL)

get_metrics <- function(cm_object, prop, method_name, cutoff_used = NA_real_, auc_roc = NA_real_, auc_pr = NA_real_) {
  
  # Caso o modelo ou a Confusion Matrix falhem (proteção contra erros)
  if (is.null(cm_object)) {
    return(tibble(
      Method = method_name,
      Cutoff = cutoff_used,
      AUC = NA_real_,
      AUC_PR = NA_real_,
      Accuracy = NA_real_,
      Kappa = NA_real_,
      Sensitivity = NA_real_,
      Specificity = NA_real_,
      Pos_Pred_Value = NA_real_,
      Neg_Pred_Value = NA_real_,
      Balanced_Accuracy = NA_real_,
      F1 = NA_real_,
      Error = "Falha no Modelo/CM"
    ))
  }
  
  # # ---------------------------
  # # AUC-ROC
  # # ---------------------------
  # roc_obj <- tryCatch(
  #   pROC::roc(labels, probs, quiet = TRUE),
  #   error = function(e) NULL
  # )
  # 
  # auc_val <- if (!is.null(roc_obj)) {
  #   as.numeric(pROC::auc(roc_obj))
  # } else {
  #   NA_real_
  # }
  # 
  # # ---------------------------
  # # AUC-PR
  # # ---------------------------
  # pr_obj <- tryCatch({
  # 
  #   PRROC::pr.curve(
  #     scores.class0 = probs[labels == "1"],
  #     scores.class1 = probs[labels == "0"],
  #     curve = FALSE
  #   )
  # 
  # }, error = function(e) NULL)
  # 
  # auc_pr_val <- if (!is.null(pr_obj)) {
  #   pr_obj$auc.integral
  # } else {
  #   NA_real_
  # }
  # 
  
  # Extração completa das métricas do caret + F1
  tibble(
    Method = method_name,
    Cutoff = cutoff_used,
    AUC               = auc_roc,
    AUC_PR            = auc_pr,
    Accuracy = as.numeric(cm_object$overall["Accuracy"]),
    Kappa = as.numeric(cm_object$overall["Kappa"]),
    Sensitivity = as.numeric(cm_object$byClass["Sensitivity"]),
    Specificity = as.numeric(cm_object$byClass["Specificity"]),
    Pos_Pred_Value = as.numeric(cm_object$byClass["Pos Pred Value"]),
    Neg_Pred_Value = as.numeric(cm_object$byClass["Neg Pred Value"]),
    Balanced_Accuracy = as.numeric(cm_object$byClass["Balanced Accuracy"]),
    F1 = as.numeric(cm_object$byClass["F1"]), # Adicionando o F1-Score
    Error = NA_character_
  )
}

compute_cutoff <- function(y_true_factor, probs, fallback) {
  # 1. Verificações de entrada
  probs <- as.numeric(probs)
  
  # 2. Garante que os níveis são 0 e 1 e lida com predições únicas
  y_true_factor <- factor(y_true_factor, levels = c("0", "1"))
  if (length(unique(probs)) <= 1) return(fallback)
  
  # 3. Cálculo ROC
  roc_obj <- tryCatch(pROC::roc(y_true_factor, probs, quiet = TRUE), error = function(e) NULL)
  if (is.null(roc_obj)) return(fallback)
  
  # 4. Extração do Threshold Otimizado
  th <- tryCatch(pROC::coords(roc_obj, "best", ret = "threshold", transpose = FALSE), 
                 error = function(e) NULL)
  
  # 5. COERÇÃO ROBUSTA (Correção principal): 
  # Usa unlist para lidar com listas/tibbles complexas e pega o primeiro valor
  th_numeric <- if (!is.null(th)) as.numeric(unlist(th))[1] else NA_real_
  
  # 6. Verificação de falha ou valores infinitos
  if (is.na(th_numeric) || is.infinite(th_numeric) || length(th_numeric) < 1) return(fallback) 
  
  return(th_numeric)
}

safe_confusion <- function(y_true, y_pred) {
  y_true <- as.character(y_true)
  y_pred <- as.character(y_pred)
  if (length(y_true) != length(y_pred)) return(NULL)
  y_true_f <- factor(y_true, levels = c("0","1"))
  y_pred_f <- factor(y_pred, levels = c("0","1"))
  cm <- tryCatch(caret::confusionMatrix(y_pred_f, y_true_f, positive = "1"), error = function(e) NULL)
  return(cm)
}

```







```{r}
# 4. Funções de log-verossimilhança para PL e RPL -----------------------------
logvero.PL <- function(param, y, x) {
  k <- ncol(x)
  beta <- param[1:k]
  lambda.star <- param[k+1]
  lambda <- exp(lambda.star)
  p <- powdist::pplogis(q = as.numeric(x %*% beta), mu = 0, sigma = 1, lambda = lambda)
  # Estabilização numérica
  p <- pmin(pmax(p, EPS_PROB), 1 - EPS_PROB)
  sum(y * log(p) + (1-y)*log(1-p))
}

logvero.RPL <- function(param, y, x) {
  k <- ncol(x)
  beta <- param[1:k]
  lambda.star <- param[k+1]
  lambda <- exp(lambda.star)
  p <- powdist::prplogis(q = as.numeric(x %*% beta), mu = 0, sigma = 1, lambda = lambda)
  # Estabilização numérica
  p <- pmin(pmax(p, EPS_PROB), 1 - EPS_PROB)
  sum(y * log(p) + (1-y)*log(1-p))
}

```






```{r}
# 5. Funções de ajuste dos modelos -----------

# --- Função para Modelos de Máxima Verossimilhança (PL e RPL) ---
fit_model_maxLik <- function(train, test, function_vero, method_name) {

  y_train <- as.numeric(as.character(train$Class))
  
  # Garantia do Intercepto: Adicionamos a coluna de 1s aqui (uma única vez)
  x_train <- cbind(Intercepto = 1, as.matrix(train[, predictors]))
  x_test  <- cbind(Intercepto = 1, as.matrix(test[, predictors]))

  # Vetor inicial: k colunas (incluindo intercepto) + 1 parâmetro lambda
  start <- rep(0, ncol(x_train) + 1)
  start[length(start)] <- log(1) # Chute inicial para lambda=1

  est <- tryCatch(
    maxLik(
      logLik  = function_vero,
      start   = start,
      y       = y_train,
      x       = x_train,
      method  = "BFGSR",
      control = list(iterlim = 100000)
    ),
    error = function(e) {
      message(paste("Erro no ajuste maxLik (", method_name, "):", e$message))
      return(NULL)
    }
  )

  # Se falhar ou gerar NA, retorna métricas vazias
  if (is.null(est) || any(is.na(est$estimate))) {
    return(get_metrics(NULL, prop_real, method_name))
  }

  # Extração dos parâmetros estimados
  beta_full <- est$estimate[1:(length(est$estimate)-1)]
  lambda    <- exp(est$estimate[length(est$estimate)])

  # Cálculo do preditor linear (eta) e probabilidade (p_pred)
  eta <- as.numeric(x_test %*% beta_full)

  p_pred <- if (identical(function_vero, logvero.PL)) {
    powdist::pplogis(eta, mu = 0, sigma = 1, lambda = lambda)
  } else {
    powdist::prplogis(eta, mu = 0, sigma = 1, lambda = lambda)
  }

  # Estabilização
  p_pred <- pmin(pmax(p_pred, EPS_PROB), 1 - EPS_PROB)
  
  # --- Cálculo das AUCs ---
  roc_obj <- tryCatch(pROC::roc(test$Class, p_pred, quiet = TRUE), error = function(e) NULL)
  v_auc_roc <- if(!is.null(roc_obj)) as.numeric(pROC::auc(roc_obj)) else NA_real_
  
  pr_obj <- tryCatch({
    PRROC::pr.curve(scores.class0 = p_pred[test$Class == "1"], 
                    scores.class1 = p_pred[test$Class == "0"], 
                    curve = FALSE)
  }, error = function(e) NULL)
  v_auc_pr <- if(!is.null(pr_obj)) pr_obj$auc.integral else NA_real_
  
  # Ponto de corte e predição
  cutoff <- compute_cutoff(test$Class, p_pred, fallback = prop_real)
  pred   <- ifelse(p_pred >= cutoff, "1", "0")

  cm <- safe_confusion(test$Class, pred)
  get_metrics(cm, prop_real, method_name, cutoff, v_auc_roc, v_auc_pr)
}

# --- Função para Logistica com Pesos (Calculados no Treino) ---
# --- Função para Logistica com Pesos Normalizados (Estabilidade Técnica) ---
fit_model_glm_weighted <- function(train, test) {
  
  y_train_num <- as.numeric(as.character(train$Class))
  
  # Cálculo de pesos normalizados (Soma dos pesos = N)
  # Isso mantém a escala da verossimilhança estável e evita NaNs
  n1 <- sum(y_train_num == 1)
  n0 <- sum(y_train_num == 0)
  n_train <- n0 + n1
  w1 <- rep(n0 / n_train, n1)
  w0 <- rep(n1 / n_train, n0)
  train$pesos_treino <- ifelse(y_train_num == 1, w1, w0)

  est_glm <- tryCatch(
    suppressWarnings(
      glm(form_glm, data = train, family = binomial(link = "logit"), weights = pesos_treino)
    ),
    error = function(e) NULL
  )

  if (is.null(est_glm)) return(get_metrics(NULL, prop_real, "Logistic_Weighted"))

  # Calculamos o VIF para garantir que os pesos não "quebraram" a estabilidade dos preditores
  vif_values <- tryCatch(car::vif(est_glm), error = function(e) NA)
  
  # Opcional: imprimir no console durante a execução para monitorar
  # cat("\nVIF para Logistic_Weighted:\n"); print(vif_values)
  
  p_pred <- as.numeric(predict(est_glm, newdata = test, type = "response"))
  p_pred <- pmin(pmax(p_pred, EPS_PROB), 1 - EPS_PROB)
  
  # --- Cálculo das AUCs ---
  roc_obj <- tryCatch(pROC::roc(test$Class, p_pred, quiet = TRUE), error = function(e) NULL)
  v_auc_roc <- if(!is.null(roc_obj)) as.numeric(pROC::auc(roc_obj)) else NA_real_
  
  pr_obj <- tryCatch({
    PRROC::pr.curve(scores.class0 = p_pred[test$Class == "1"], 
                    scores.class1 = p_pred[test$Class == "0"], 
                    curve = FALSE)
  }, error = function(e) NULL)
  v_auc_pr <- if(!is.null(pr_obj)) pr_obj$auc.integral else NA_real_

  cutoff <- compute_cutoff(test$Class, p_pred, fallback = prop_real)
  pred   <- ifelse(p_pred >= cutoff, "1", "0")

  cm <- safe_confusion(test$Class, pred)
  get_metrics(cm, prop_real, "Logistic_Weighted", cutoff, v_auc_roc, v_auc_pr)
}

# --- Função para Logistica Padrão ---
fit_model_glm_standard <- function(train, test) {

  est_glm <- tryCatch(
    suppressWarnings(glm(form_glm, data = train, family = binomial(link = "logit"))),
    error = function(e) NULL
  )

  if (is.null(est_glm)) return(get_metrics(NULL, prop_real, "Logistic_Standard"))

  p_pred <- as.numeric(predict(est_glm, newdata = test, type = "response"))
  p_pred <- pmin(pmax(p_pred, EPS_PROB), 1 - EPS_PROB)
  
  # --- Cálculo das AUCs ---
  roc_obj <- tryCatch(pROC::roc(test$Class, p_pred, quiet = TRUE), error = function(e) NULL)
  v_auc_roc <- if(!is.null(roc_obj)) as.numeric(pROC::auc(roc_obj)) else NA_real_
  
  pr_obj <- tryCatch({
    PRROC::pr.curve(scores.class0 = p_pred[test$Class == "1"], 
                    scores.class1 = p_pred[test$Class == "0"], 
                    curve = FALSE)
  }, error = function(e) NULL)
  v_auc_pr <- if(!is.null(pr_obj)) pr_obj$auc.integral else NA_real_

  cutoff <- compute_cutoff(test$Class, p_pred, fallback = prop_real)
  pred   <- ifelse(p_pred >= cutoff, "1", "0")

  cm <- safe_confusion(test$Class, pred)
  get_metrics(cm, prop_real, "Logistic_Standard", cutoff, v_auc_roc, v_auc_pr)
}

```







```{r}
# 6. Execução Única (Substitui o Runner de Monte Carlo) -----------------------

# 6.1. Partição Única de Dados
# Usamos strata = Class para garantir que o teste tenha a mesma proporção de fraudes
split <- rsample::initial_split(dados, prop = 0.75, strata = Class)
train <- rsample::training(split)
test  <- rsample::testing(split)

message(sprintf("\n🚀 Iniciando Execução Única - Creditcard | Prevalência: %.5f", prop_real))
time_start <- Sys.time()

## 6.2. Ajuste dos Modelos em Sequência
# Chamamos as funções simplificadas que definimos no Bloco 6.
# Como não há mais loop, não passamos o 'sim_num'.
results <- bind_rows(
  
  cat("  -> Ajustando Logistic_Standard...\n"),
  fit_model_glm_standard(train, test),
  
  cat("  -> Ajustando Logistic_Weighted (Pesos no Treino)...\n"),
  fit_model_glm_weighted(train, test),
  
  cat("  -> Ajustando Logistic_PL (Potencializada - Máxima Verossimilhança)...\n"),
  fit_model_maxLik(train, test, function_vero = logvero.PL, method_name = "Logistic_PL"),
  
  cat("  -> Ajustando Logistic_RPL (Potencializada Reversa)...\n"),
  fit_model_maxLik(train, test, function_vero = logvero.RPL, method_name = "Logistic_RPL")
)

time_end <- Sys.time()
cat("\n🏁 Execução finalizada com sucesso!\n")
print(time_end - time_start)

# Organização final da tabela
results <- results %>% mutate(Method = as.character(Method))
results

```







```{r}
# 7. Agregação e Resumo dos Resultados (Execução Única) -----------------------

metric_summary <- results %>%
  group_by(Method) %>%
  summarise(
    AUC                  = mean(AUC, na.rm = TRUE),
    AUC_PR               = mean(AUC_PR, na.rm = TRUE),
    Acuracia             = mean(Accuracy, na.rm = TRUE),
    Acuracia_Balanceada  = mean(Balanced_Accuracy, na.rm = TRUE),
    Sensibilidade        = mean(Sensitivity, na.rm = TRUE),
    Especificidade       = mean(Specificity, na.rm = TRUE),
    Precisao             = mean(Pos_Pred_Value, na.rm = TRUE),
    NPV                  = mean(Neg_Pred_Value, na.rm = TRUE), # Adicionado aqui
    Kappa                = mean(Kappa, na.rm = TRUE),
    F1_Score             = mean(F1, na.rm = TRUE),
    Cutoff_Otimizado     = mean(Cutoff, na.rm = TRUE),
    .groups = 'drop'
  )

# Exibir os resultados para conferência
print(metric_summary)
```







```{r}

# 8. Gráficos Comparativos (Todas as Métricas Calculadas) ----------------------

# 1. Transformar os dados para o formato longo incluindo TODAS as métricas
metric_long <- metric_summary %>%
  pivot_longer(
    cols = c(AUC, AUC_PR, Acuracia, Acuracia_Balanceada, Sensibilidade, Especificidade, 
             Precisao, NPV, Kappa, F1_Score), # Nomes batendo com o summarise
    names_to = "Metrica", 
    values_to = "Valor"
  ) %>%
  # Ordenando para o gráfico ficar profissional
  mutate(Metrica = factor(Metrica, 
                          levels = c("AUC", "AUC_PR", "Acuracia", "Acuracia_Balanceada", "Sensibilidade", 
                                     "Especificidade", "Precisao", "NPV", 
                                     "Kappa", "F1_Score")))

# 2. Gerar o gráfico facetado (Versão sem cortes)
ggplot(metric_long, aes(x = Method, y = Valor, fill = Method)) +
  geom_col(show.legend = FALSE, alpha = 0.85) +
  # Rótulos com vjust um pouco mais baixo para segurança
  geom_text(aes(label = ifelse(is.na(Valor), "NA", scales::percent(Valor, accuracy = 0.1))),
            vjust = -0.5, size = 2.8) + 
  # facet_wrap com escalas livres
  facet_wrap(~ Metrica, ncol = 2, scales = "free_y") +  
  # expand adiciona 15% de espaço extra no topo de CADA painel automaticamente
  scale_y_continuous(labels = scales::percent, 
                     expand = expansion(mult = c(0, 0.15))) + 
  labs(
    title = "Avaliação Multidimensional dos Modelos - Creditcard Fraud",
    subtitle = "Comparação de Densempenho",
    x = "Método", 
    y = "Desempenho (%)"
  ) +
  theme_minimal() + 
  theme(
    axis.text.x = element_text(angle = 40, hjust = 1, size = 8),
    strip.text = element_text(face = "bold", size = 10),
    panel.spacing = unit(1.2, "lines"),
    # AQUI A MÁGICA: impede o corte de elementos fora do painel
    plot.margin = margin(10, 10, 10, 10) 
  ) +
  # Garante que o texto 'voe' acima da linha se necessário
  coord_cartesian(clip = "off")

ggsave("Grafico_Acuracia_Final.png", width = 10, height = 12, dpi = 300)

```







```{r}
# 9. Salvamento de resultados ------------------------------------------------
# Salvamos a tabela completa (com todas as métricas) em CSV
readr::write_csv(metric_summary, "metricas_finais__creditcard.csv")

message("✅ Script finalizado! Os resultados completos foram salvos em 'metricas_finais_tese_creditcard.csv'")

```
